{
  "name": "xlayer-defi-agent",
  "version": "1.0.0",
  "description": "AI Agent for DeFi on X Layer",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network xlayer",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xlayer-testnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "web3": "^4.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0"
  }
}
